{
  "name": "mq-city-builder",
  "private": true,
  "version": "1.0.0-beta.1",
  "type": "module",
  "description": "MQ City Builder — premium mobile-first low-poly 3D city builder (Three.js + TypeScript). Beta 1.0.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.11"
  }
}
